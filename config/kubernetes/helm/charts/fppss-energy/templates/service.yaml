apiVersion: v1
kind: Service
metadata:
  name: {{ include "fppss-energy.fullname" . }}
  labels:
    {{- include "fppss-energy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fppss-energy.selectorLabels" . | nindent 4 }}
