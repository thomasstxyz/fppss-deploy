apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fppss-frontend
  namespace: fppss-energy
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: fppss-frontend
            port:
              number: 80
