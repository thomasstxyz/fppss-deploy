apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fppss-energy.fullname" . }}-test-connection"
  labels:
    {{- include "fppss-energy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fppss-energy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
